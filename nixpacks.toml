[phases.setup]
nixPkgs = ["python3", "ffmpeg", "curl"]

[phases.install]
cmds = ["npm install", "pip3 install yt-dlp || curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod a+rx /usr/local/bin/yt-dlp"]
